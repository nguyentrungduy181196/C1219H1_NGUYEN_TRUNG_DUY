let a = Number.parseInt(prompt("so thu nhat: "));
let b = Number.parseInt(prompt("so thu hai: "));
chuyendoi(a,b);
function chuyendoi(a,b) {
    let c = a;
    a = b;
    alert("so da duoc doi: " + a  + c);
}
